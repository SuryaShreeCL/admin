import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
  title: {
    fontStyle: "Poppins",
    fontStyle: "normal",
    fontSize: "18px",
  },
  question: {
    fontStyle: "Montserrat",
    fontSize: "16.8px",
    fontStyle: "normal",
  },
}));
